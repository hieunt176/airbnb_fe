import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  root: {
    backgroundColor: "#fff",
  },

  gridContainer: {
    width: "1080px !important",
    margin: "120px auto",
  },
});
