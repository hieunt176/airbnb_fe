import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  card: {
    maxWidth: 504,
    padding: 32,
  },

  action: {
    textDecoration: "none",
    fontSize: 16,
    color: "#484848",
    fontWeight: "800",
  },
});
