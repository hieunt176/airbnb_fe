import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  root: {
    backgroundColor: "#000",
    padding: "120px 0",
  },

  contain: {
    width: 1080,
    margin: "0 auto",
  },
});
